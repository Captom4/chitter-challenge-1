source 'https://rubygems.org'

ruby '2.5.0'

gem 'activerecord' # says we can use it in the md file on the mail repository.
gem 'bcrypt'
gem 'bootstrap-sass'
# gem 'bootstrap_form'
gem 'pg'
gem 'rake'
gem 'require_all'
gem 'rubocop'
gem 'shotgun'
gem 'simple_form'
gem 'sinatra'
gem 'sinatra-activerecord', :require => 'sinatra/activerecord'
gem 'sinatra-flash'
gem 'thin'
gem 'uglifier', '>= 1.3.0'

group :development, :test do
  gem 'byebug', platforms: %i[mri mingw x64_mingw]
  gem 'simplecov'
  gem 'simplecov-console'
end

group :development do
  gem 'listen', '>= 3.0.5', '< 3.2'
  gem 'spring'
  gem 'spring-watcher-listen', '~> 2.0.0'
  gem 'sqlite3'
  gem 'web-console', '>= 3.3.0'
end

group :test do
  gem 'capybara'
  gem 'database_cleaner'
  gem 'rspec'
end
